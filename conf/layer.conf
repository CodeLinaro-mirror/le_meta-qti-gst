BBFILES += "${LAYERDIR}/recipes-*/*/*.bb ${LAYERDIR}/recipes-*/*/*.bbappend"
CONFDIR := "${@os.path.dirname(d.getVar('FILE', 1))}"
BBPATH .= ":${LAYERDIR}"
BBFILE_COLLECTIONS += "qti-gst"
BBFILE_PATTERN_qti-gst := "^${LAYERDIR}/"
BBFILE_PRIORITY_qti-gst = "17"
LAYERSERIES_COMPAT_qti-gst = "kirkstone scarthgap walnascar whinlatter"

# add packagegroup-qcom-gst to the base image
CORE_IMAGE_EXTRA_INSTALL:append = " packagegroup-qcom-gst"
