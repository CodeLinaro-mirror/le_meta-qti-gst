BBFILES += "${@bb.utils.contains('MACHINE', 'sxrneo-ar-sg1', '${LAYERDIR}/recipes/*/*.bb ${LAYERDIR}/recipes/*/*.bbappend', '', d)}"
BBFILES += "${@bb.utils.contains('MACHINE', 'ar-sg1', '${LAYERDIR}/recipes/*/*.bb ${LAYERDIR}/recipes/*/*.bbappend', '', d)}"
BBPATH .= ":${LAYERDIR}"
BBFILE_COLLECTIONS += "qti-gst"
BBFILE_PATTERN_qti-gst := "^${LAYERDIR}/"
BBFILE_PRIORITY_qti-gst = "17"

LAYERDEPENDS_qti-gst = "core"
LAYERSERIES_COMPAT_qti-gst = "dunfell kirkstone"

# do not error out on bbappends for missing recipes
BB_DANGLINGAPPENDS_WARNONLY = "true"
