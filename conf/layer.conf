BBFILES += "${LAYERDIR}/recipes/*.bb ${LAYERDIR}/recipes/*.bbappend ${LAYERDIR}/recipes-*/*/*.bb ${LAYERDIR}/recipes-*/*/*.bbappend"
BBPATH .= ":${LAYERDIR}"
BBFILE_COLLECTIONS += "gst"
BBFILE_PATTERN_gst := "^${LAYERDIR}/"

# msm = meta-qti-bsp
LAYERDEPENDS_qsap = "msm"
