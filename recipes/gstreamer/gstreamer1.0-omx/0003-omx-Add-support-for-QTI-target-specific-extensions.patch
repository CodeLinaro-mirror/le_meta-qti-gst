From d42a66f2bea76f03484f1a45fb32f2053800edce Mon Sep 17 00:00:00 2001
From: "Petar G. Georgiev" <quic_petarg@quicinc.com>
Date: Mon, 22 Aug 2022 19:17:04 +0300
Subject: [PATCH] omx: Add support for QTI target specific extensions

Signed-off-by: Petar G. Georgiev <quic_petarg@quicinc.com>
Signed-off-by: Aashish Patel <quic_paashish@quicinc.com>
Signed-off-by: Amrutha Sai Gattu <quic_agattu@quicinc.com>
Signed-off-by: Shyam Komakula <quic_skomakul@quicinc.com>
Signed-off-by: Xinzheng Long <quic_xinzlong@quicinc.com>
---
 omx/gstomx.c         |  14 +-
 omx/gstomxaacdec.c   |  13 ++
 omx/gstomxaudiodec.c |   1 +
 omx/gstomxh264enc.c  |  20 +++
 omx/gstomxh265enc.c  | 148 +++++++++++++++++
 omx/gstomxh265enc.h  |   5 +-
 omx/gstomxvideoenc.c | 384 +++++++++++++++++++++++++++++++++++++++++++
 omx/gstomxvideoenc.h |  11 ++
 8 files changed, 592 insertions(+), 4 deletions(-)

diff --git a/omx/gstomx.c b/omx/gstomx.c
index 0c88818..7718f8c 100644
--- a/omx/gstomx.c
+++ b/omx/gstomx.c
@@ -864,9 +864,18 @@ EventHandler (OMX_HANDLETYPE hComponent, OMX_PTR pAppData, OMX_EVENTTYPE eEvent,
     }
     case OMX_EventPortSettingsChanged:
     {
-      GstOMXMessage *msg = g_slice_new (GstOMXMessage);
+      GstOMXMessage *msg;
       OMX_U32 index;
 
+#ifdef USE_OMX_TARGET_QTI
+      if (nData2 == OMX_IndexConfigCommonOutputCrop ||
+          nData2 == OMX_QTIIndexConfigDescribeColorAspects) {
+        GST_DEBUG_OBJECT (comp->parent, "%s ignore OMX_EventPortSettingsChanged"
+            " index 0x%08x (port index: %u)", comp->name, nData2, nData1);
+        break;
+      }
+#endif
+
       if (!(comp->hacks &
               GST_OMX_HACK_EVENT_PORT_SETTINGS_CHANGED_NDATA_PARAMETER_SWAP)) {
         index = nData1;
@@ -874,13 +883,12 @@ EventHandler (OMX_HANDLETYPE hComponent, OMX_PTR pAppData, OMX_EVENTTYPE eEvent,
         index = nData2;
       }
 
-
       if (index == 0
           && (comp->hacks &
               GST_OMX_HACK_EVENT_PORT_SETTINGS_CHANGED_PORT_0_TO_1))
         index = 1;
 
-
+      msg = g_slice_new (GstOMXMessage);
       msg->type = GST_OMX_MESSAGE_PORT_SETTINGS_CHANGED;
       msg->content.port_settings_changed.port = index;
       GST_DEBUG_OBJECT (comp->parent, "%s settings changed (port index: %u)",
diff --git a/omx/gstomxaacdec.c b/omx/gstomxaacdec.c
index 9606b7f..a4ad614 100644
--- a/omx/gstomxaacdec.c
+++ b/omx/gstomxaacdec.c
@@ -162,6 +162,19 @@ gst_omx_aac_dec_set_format (GstOMXAudioDec * dec, GstOMXPort * port,
     return FALSE;
   }
 
+#ifdef USE_OMX_TARGET_QTI
+  QOMX_AUDIO_CONFIG_DUALMONOTYPE dualmono;
+  GST_OMX_INIT_STRUCT (&dualmono);
+  dualmono.eChannelConfig = OMX_AUDIO_DUAL_MONO_MODE_DEFAULT;
+  err =
+      gst_omx_component_set_config (dec->dec, QOMX_IndexConfigAudioDualMono,
+      &dualmono);
+  if (err != OMX_ErrorNone) {
+      GST_ERROR_OBJECT (self, "Error setting AAC config: QOMX_IndexConfigAudioDualMono (0x%08x)", err);
+      return FALSE;
+  }
+#endif
+
   return TRUE;
 }
 
diff --git a/omx/gstomxaudiodec.c b/omx/gstomxaudiodec.c
index 8275b72..3f375c8 100644
--- a/omx/gstomxaudiodec.c
+++ b/omx/gstomxaudiodec.c
@@ -350,6 +350,7 @@ gst_omx_audio_dec_loop (GstOMXAudioDec * self)
 
     GST_OMX_INIT_STRUCT (&pcm_param);
     pcm_param.nPortIndex = self->dec_out_port->index;
+    pcm_param.eEndian = OMX_EndianLittle;
     err =
         gst_omx_component_get_parameter (self->dec, OMX_IndexParamAudioPcm,
         &pcm_param);
diff --git a/omx/gstomxh264enc.c b/omx/gstomxh264enc.c
index f75fff0..ccdfd23 100644
--- a/omx/gstomxh264enc.c
+++ b/omx/gstomxh264enc.c
@@ -459,6 +459,10 @@ update_param_avc (GstOMXH264Enc * self,
     param.eLoopFilterMode = self->loop_filter_mode;
   }
 
+  if (profile == OMX_VIDEO_AVCProfileHigh) {
+    param.bDirect8x8Inference = OMX_TRUE;
+  }
+
   err =
       gst_omx_component_set_parameter (GST_OMX_VIDEO_ENC (self)->enc,
       OMX_IndexParamVideoAvc, &param);
@@ -624,6 +628,22 @@ gst_omx_h264_enc_set_format (GstOMXVideoEnc * enc, GstOMXPort * port,
   }
 #endif
 
+#ifdef USE_OMX_TARGET_QTI
+  OMX_QCOM_VIDEO_PARAM_VUI_TIMING_INFO vui_timing_info;
+
+  GST_OMX_INIT_STRUCT(&vui_timing_info);
+  vui_timing_info.bEnable = OMX_TRUE;
+  err =
+      gst_omx_component_set_parameter (GST_OMX_VIDEO_ENC (self)->enc,
+      OMX_QcomIndexParamH264VUITimingInfo, &vui_timing_info);
+  if (err != OMX_ErrorNone) {
+    GST_ERROR_OBJECT (self,
+        "can't set OMX_QcomIndexParamH264VUITimingInfo %s (0x%08x)",
+        gst_omx_error_to_string (err), err);
+    return FALSE;
+  }
+#endif
+
   /* Configure GOP pattern */
   if (self->periodicty_idr !=
       GST_OMX_H264_VIDEO_ENC_PERIODICITY_OF_IDR_FRAMES_DEFAULT
diff --git a/omx/gstomxh265enc.c b/omx/gstomxh265enc.c
index 9989420..b46b41c 100644
--- a/omx/gstomxh265enc.c
+++ b/omx/gstomxh265enc.c
@@ -127,6 +127,23 @@ gst_omx_h265_enc_class_init (GstOMXH265EncClass * klass)
           G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS |
           GST_PARAM_MUTABLE_READY));
 
+#ifdef USE_OMX_TARGET_QTI
+  g_object_class_install_property (gobject_class, PROP_PERIODICITYOFIDRFRAMES,
+      g_param_spec_uint ("periodicity-idr", "IDR periodicity",
+          "Periodicity of IDR frames (0xffffffff=component default)",
+          0, G_MAXUINT,
+          GST_OMX_H265_VIDEO_ENC_PERIODICITY_OF_IDR_FRAMES_DEFAULT,
+          G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS |
+          GST_PARAM_MUTABLE_READY));
+
+  g_object_class_install_property (gobject_class, PROP_B_FRAMES,
+      g_param_spec_uint ("b-frames", "Number of B-frames",
+          "Number of B-frames between two consecutive I-frames (0xffffffff=component default)",
+          0, G_MAXUINT, GST_OMX_H265_VIDEO_ENC_B_FRAMES_DEFAULT,
+          G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS |
+          GST_PARAM_MUTABLE_READY));
+#endif
+
 #ifdef USE_OMX_TARGET_ZYNQ_USCALE_PLUS
   g_object_class_install_property (gobject_class, PROP_PERIODICITYOFIDRFRAMES,
       g_param_spec_uint ("periodicity-idr", "IDR periodicity",
@@ -186,6 +203,14 @@ gst_omx_h265_enc_set_property (GObject * object, guint prop_id,
     case PROP_INTERVALOFCODINGINTRAFRAMES:
       self->interval_intraframes = g_value_get_uint (value);
       break;
+#ifdef USE_OMX_TARGET_QTI
+    case PROP_PERIODICITYOFIDRFRAMES:
+      self->periodicity_idr = g_value_get_uint (value);
+      break;
+    case PROP_B_FRAMES:
+      self->b_frames = g_value_get_uint (value);
+      break;
+#endif
 #ifdef USE_OMX_TARGET_ZYNQ_USCALE_PLUS
     case PROP_PERIODICITYOFIDRFRAMES:
       self->periodicity_idr = g_value_get_uint (value);
@@ -216,6 +241,14 @@ gst_omx_h265_enc_get_property (GObject * object, guint prop_id, GValue * value,
     case PROP_INTERVALOFCODINGINTRAFRAMES:
       g_value_set_uint (value, self->interval_intraframes);
       break;
+#ifdef USE_OMX_TARGET_QTI
+    case PROP_PERIODICITYOFIDRFRAMES:
+      g_value_set_uint (value, self->periodicity_idr);
+      break;
+    case PROP_B_FRAMES:
+      g_value_set_uint (value, self->b_frames);
+      break;
+#endif
 #ifdef USE_OMX_TARGET_ZYNQ_USCALE_PLUS
     case PROP_PERIODICITYOFIDRFRAMES:
       g_value_set_uint (value, self->periodicity_idr);
@@ -241,6 +274,11 @@ gst_omx_h265_enc_init (GstOMXH265Enc * self)
 {
   self->interval_intraframes =
       GST_OMX_H265_VIDEO_ENC_INTERVAL_OF_CODING_INTRA_FRAMES_DEFAULT;
+#ifdef USE_OMX_TARGET_QTI
+  self->periodicity_idr =
+      GST_OMX_H265_VIDEO_ENC_PERIODICITY_OF_IDR_FRAMES_DEFAULT;
+  self->b_frames = GST_OMX_H265_VIDEO_ENC_B_FRAMES_DEFAULT;
+#endif
 #ifdef USE_OMX_TARGET_ZYNQ_USCALE_PLUS
   self->periodicity_idr =
       GST_OMX_H265_VIDEO_ENC_PERIODICITY_OF_IDR_FRAMES_DEFAULT;
@@ -405,6 +443,86 @@ update_param_hevc (GstOMXH265Enc * self,
   return TRUE;
 }
 
+#ifdef USE_OMX_TARGET_QTI
+static gboolean
+set_intra_period (GstOMXH265Enc * self)
+{
+  QOMX_VIDEO_INTRAPERIODTYPE param;
+  OMX_ERRORTYPE err;
+
+  GST_OMX_INIT_STRUCT (&param);
+  param.nPortIndex = GST_OMX_VIDEO_ENC (self)->enc_out_port->index;
+
+  err =
+      gst_omx_component_get_config (GST_OMX_VIDEO_ENC (self)->enc,
+      QOMX_IndexConfigVideoIntraperiod, &param);
+  if (err == OMX_ErrorUnsupportedIndex) {
+    GST_WARNING_OBJECT (self,
+        "QOMX_IndexConfigVideoIntraperiod not supported by component");
+    return TRUE;
+  } else if (err != OMX_ErrorNone) {
+    GST_ERROR_OBJECT (self,
+        "can't get QOMX_IndexConfigVideoIntraperiod %s (0x%08x)",
+        gst_omx_error_to_string (err), err);
+    return FALSE;
+  }
+
+  GST_DEBUG_OBJECT (self, "nPFrames:%u, nIDRPeriod:%u",
+      (guint) param.nPFrames, (guint) param.nIDRPeriod);
+
+  if (self->periodicity_idr !=
+      GST_OMX_H265_VIDEO_ENC_PERIODICITY_OF_IDR_FRAMES_DEFAULT) {
+    param.nIDRPeriod = self->periodicity_idr;
+  }
+
+  if (self->interval_intraframes !=
+      GST_OMX_H265_VIDEO_ENC_INTERVAL_OF_CODING_INTRA_FRAMES_DEFAULT) {
+    /* This OMX API doesn't allow us to specify the number of B-frames.
+     * So if user requested one we have to rely on update_param_hevc()
+     * to configure the intraframes interval so it can take the
+     * B-frames into account. */
+    if (self->b_frames == GST_OMX_H265_VIDEO_ENC_B_FRAMES_DEFAULT)
+      param.nPFrames = self->interval_intraframes;
+
+    /* If user specified a specific number of B-frames, reduce the number of
+     * P-frames by this amount. If not ensure there is no B-frame to have the
+     * requested GOP length. */
+    if (self->b_frames != GST_OMX_H265_VIDEO_ENC_B_FRAMES_DEFAULT) {
+      if (self->b_frames > self->interval_intraframes) {
+        GST_ERROR_OBJECT (self,
+            "The interval_intraframes perdiod (%u) needs to be higher than the number of B-frames (%u)",
+            self->interval_intraframes, self->b_frames);
+        return FALSE;
+      }
+      param.nPFrames -= self->b_frames;
+    } else {
+      param.nBFrames = 0;
+    }
+  }
+
+  if (self->b_frames != GST_OMX_H265_VIDEO_ENC_B_FRAMES_DEFAULT)
+    param.nBFrames = self->b_frames;
+
+  err =
+      gst_omx_component_set_config (GST_OMX_VIDEO_ENC (self)->enc,
+      QOMX_IndexConfigVideoIntraperiod, &param);
+
+  if (err == OMX_ErrorUnsupportedIndex) {
+    GST_WARNING_OBJECT (self,
+        "Setting QOMX_IndexConfigVideoIntraperiod not supported by component");
+    return TRUE;
+  } else if (err != OMX_ErrorNone) {
+    GST_ERROR_OBJECT (self,
+        "Error setting HEVC settings (IDR period %u, P-frames %u and B-frames %u): %s (0x%08x)",
+        (guint) param.nIDRPeriod, (guint) param.nPFrames, (guint) param.nBFrames,
+        gst_omx_error_to_string (err), err);
+    return FALSE;
+  }
+
+  return TRUE;
+ }
+#endif
+
 #ifdef USE_OMX_TARGET_ZYNQ_USCALE_PLUS
 static gboolean
 set_intra_period (GstOMXH265Enc * self)
@@ -447,6 +565,16 @@ gst_omx_h265_enc_set_format (GstOMXVideoEnc * enc, GstOMXPort * port,
   OMX_VIDEO_HEVCPROFILETYPE profile = OMX_VIDEO_HEVCProfileUnknown;
   OMX_VIDEO_HEVCLEVELTYPE level = OMX_VIDEO_HEVCLevelUnknown;
 
+#ifdef USE_OMX_TARGET_QTI
+  if (self->periodicity_idr !=
+      GST_OMX_H265_VIDEO_ENC_PERIODICITY_OF_IDR_FRAMES_DEFAULT
+      || self->interval_intraframes !=
+      GST_OMX_H265_VIDEO_ENC_INTERVAL_OF_CODING_INTRA_FRAMES_DEFAULT
+      || self->b_frames != GST_OMX_H265_VIDEO_ENC_B_FRAMES_DEFAULT) {
+    set_intra_period (self);
+  }
+#endif
+
 #ifdef USE_OMX_TARGET_ZYNQ_USCALE_PLUS
   if (self->periodicity_idr !=
       GST_OMX_H265_VIDEO_ENC_PERIODICITY_OF_IDR_FRAMES_DEFAULT)
@@ -603,6 +731,26 @@ gst_omx_h265_enc_get_caps (GstOMXVideoEnc * enc, GstOMXPort * port,
         tier = "main";
         level = "6.2";
         break;
+      case OMX_VIDEO_HEVCHighTierLevel1:
+        tier = "hight";
+        level = "1";
+        break;
+      case OMX_VIDEO_HEVCHighTierLevel2:
+        tier = "high";
+        level = "2";
+        break;
+      case OMX_VIDEO_HEVCHighTierLevel21:
+        tier = "high";
+        level = "2.1";
+        break;
+      case OMX_VIDEO_HEVCHighTierLevel3:
+        tier = "high";
+        level = "3";
+        break;
+      case OMX_VIDEO_HEVCHighTierLevel31:
+        tier = "high";
+        level = "3.1";
+        break;
       case OMX_VIDEO_HEVCHighTierLevel4:
         tier = "high";
         level = "4";
diff --git a/omx/gstomxh265enc.h b/omx/gstomxh265enc.h
index c53be18..d8fec7f 100644
--- a/omx/gstomxh265enc.h
+++ b/omx/gstomxh265enc.h
@@ -46,9 +46,12 @@ typedef struct _GstOMXH265EncClass GstOMXH265EncClass;
 struct _GstOMXH265Enc
 {
   GstOMXVideoEnc parent;
-
   /* properties */
   guint32 interval_intraframes;
+#ifdef USE_OMX_TARGET_QTI
+  guint32 periodicity_idr;
+  guint32 b_frames;
+#endif
 #ifdef USE_OMX_TARGET_ZYNQ_USCALE_PLUS
   guint32 periodicity_idr;
   guint32 b_frames;
diff --git a/omx/gstomxvideoenc.c b/omx/gstomxvideoenc.c
index 8bf9a46..bce9774 100644
--- a/omx/gstomxvideoenc.c
+++ b/omx/gstomxvideoenc.c
@@ -51,10 +51,17 @@ gst_omx_video_enc_control_rate_get_type (void)
       {OMX_Video_ControlRateDisable, "Disable", "disable"},
       {OMX_Video_ControlRateVariable, "Variable", "variable"},
       {OMX_Video_ControlRateConstant, "Constant", "constant"},
+#ifdef USE_OMX_TARGET_QTI
+      {QOMX_Video_ControlRateMaxBitrate, "Max Bitrate", "max-bitrate"},
+#endif
       {OMX_Video_ControlRateVariableSkipFrames, "Variable Skip Frames",
           "variable-skip-frames"},
       {OMX_Video_ControlRateConstantSkipFrames, "Constant Skip Frames",
           "constant-skip-frames"},
+#ifdef USE_OMX_TARGET_QTI
+      {QOMX_Video_ControlRateMaxBitrateSkipFrames, "Max Bitrate Skip Frames",
+          "max-bitrate-skip-frames"},
+#endif
 #ifdef USE_OMX_TARGET_ZYNQ_USCALE_PLUS
       {OMX_ALG_Video_ControlRateLowLatency, "Low Latency", "low-latency"},
 #endif
@@ -67,6 +74,26 @@ gst_omx_video_enc_control_rate_get_type (void)
   return qtype;
 }
 
+#define GST_TYPE_OMX_VIDEO_ENC_ROTATE (gst_omx_video_enc_rotate_get_type ())
+static GType
+gst_omx_video_enc_rotate_get_type (void)
+{
+  static GType qtype = 0;
+
+  if (qtype == 0) {
+    static const GEnumValue values[] = {
+      {0, "No rotation", "none" },
+      {1, "Rotate 90 degrees clockwise", "90CW"},
+      {2, "Rotate 180 degrees", "180"},
+      {3, "Rotate 90 degrees counter-clockwise", "90CCW"},
+      {0, NULL, NULL},
+    };
+
+    qtype = g_enum_register_static ("GstOmxVideoEncRotate", values);
+  }
+  return qtype;
+}
+
 #ifdef USE_OMX_TARGET_ZYNQ_USCALE_PLUS
 #define GST_TYPE_OMX_VIDEO_ENC_QP_MODE (gst_omx_video_enc_qp_mode_get_type ())
 static GType
@@ -242,9 +269,18 @@ enum
   PROP_0,
   PROP_CONTROL_RATE,
   PROP_TARGET_BITRATE,
+  PROP_ROTATE,
   PROP_QUANT_I_FRAMES,
   PROP_QUANT_P_FRAMES,
   PROP_QUANT_B_FRAMES,
+  PROP_MIN_QUANT_I_FRAMES,
+  PROP_MAX_QUANT_I_FRAMES,
+  PROP_MIN_QUANT_P_FRAMES,
+  PROP_MAX_QUANT_P_FRAMES,
+  PROP_MIN_QUANT_B_FRAMES,
+  PROP_MAX_QUANT_B_FRAMES,
+  PROP_NUM_LTR_FRAMES,
+  PROP_NUM_HIER_LAYERS,
   PROP_QP_MODE,
   PROP_MIN_QP,
   PROP_MAX_QP,
@@ -266,6 +302,7 @@ enum
 /* FIXME: Better defaults */
 #define GST_OMX_VIDEO_ENC_CONTROL_RATE_DEFAULT (0xffffffff)
 #define GST_OMX_VIDEO_ENC_TARGET_BITRATE_DEFAULT (0xffffffff)
+#define GST_OMX_VIDEO_ENC_ROTATE_DEFAULT (0)
 #define GST_OMX_VIDEO_ENC_QUANT_I_FRAMES_DEFAULT (0xffffffff)
 #define GST_OMX_VIDEO_ENC_QUANT_P_FRAMES_DEFAULT (0xffffffff)
 #define GST_OMX_VIDEO_ENC_QUANT_B_FRAMES_DEFAULT (0xffffffff)
@@ -285,6 +322,8 @@ enum
 #define GST_OMX_VIDEO_ENC_SLICE_SIZE_DEFAULT (0)
 #define GST_OMX_VIDEO_ENC_DEPENDENT_SLICE_DEFAULT (FALSE)
 #define GST_OMX_VIDEO_ENC_DEFAULT_ROI_QUALITY OMX_ALG_ROI_QUALITY_HIGH
+#define GST_OMX_VIDEO_ENC_NUM_LTR_FRAMES_DEFAULT (0xffffffff)
+#define GST_OMX_VIDEO_ENC_NUM_HIER_LAYERS_DEFAULT (0xffffffff)
 
 /* class initialization */
 #define do_init \
@@ -324,6 +363,14 @@ gst_omx_video_enc_class_init (GstOMXVideoEncClass * klass)
           G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS |
           GST_PARAM_MUTABLE_PLAYING));
 
+  g_object_class_install_property (gobject_class, PROP_ROTATE,
+      g_param_spec_enum ("rotate", "Rotate",
+          "Rotate input image before encoding",
+          GST_TYPE_OMX_VIDEO_ENC_ROTATE,
+          GST_OMX_VIDEO_ENC_ROTATE_DEFAULT,
+          G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS |
+          GST_PARAM_MUTABLE_READY));
+
   g_object_class_install_property (gobject_class, PROP_QUANT_I_FRAMES,
       g_param_spec_uint ("quant-i-frames", "I-Frame Quantization",
           "Quantization parameter for I-frames (0xffffffff=component default)",
@@ -345,6 +392,64 @@ gst_omx_video_enc_class_init (GstOMXVideoEncClass * klass)
           G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS |
           GST_PARAM_MUTABLE_READY));
 
+#ifdef USE_OMX_TARGET_QTI
+  g_object_class_install_property (gobject_class, PROP_MIN_QUANT_I_FRAMES,
+      g_param_spec_uint ("min-quant-i-frames", "min I-Frame Quantization",
+          "Minimum quantization parameter allowed for I-frames",
+          0, 51, GST_OMX_VIDEO_ENC_MIN_QP_DEFAULT,
+          G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS |
+          GST_PARAM_MUTABLE_READY));
+
+  g_object_class_install_property (gobject_class, PROP_MAX_QUANT_I_FRAMES,
+      g_param_spec_uint ("max-quant-i-frames", "max I-Frame Quantization",
+          "Maximum quantization parameter allowed for I-frames",
+          0, 51, GST_OMX_VIDEO_ENC_MAX_QP_DEFAULT,
+          G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS |
+          GST_PARAM_MUTABLE_READY));
+
+  g_object_class_install_property (gobject_class, PROP_MIN_QUANT_P_FRAMES,
+      g_param_spec_uint ("min-quant-p-frames", "min P-Frame Quantization",
+          "Minimum quantization parameter allowed for P-frames",
+          0, 51, GST_OMX_VIDEO_ENC_MIN_QP_DEFAULT,
+          G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS |
+          GST_PARAM_MUTABLE_READY));
+
+  g_object_class_install_property (gobject_class, PROP_MAX_QUANT_P_FRAMES,
+      g_param_spec_uint ("max-quant-p-frames", "max P-Frame Quantization",
+          "Maximum quantization parameter allowed for P-frames",
+          0, 51, GST_OMX_VIDEO_ENC_MAX_QP_DEFAULT,
+          G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS |
+          GST_PARAM_MUTABLE_READY));
+
+  g_object_class_install_property (gobject_class, PROP_MIN_QUANT_B_FRAMES,
+      g_param_spec_uint ("min-quant-b-frames", "min B-Frame Quantization",
+          "Minimum quantization parameter allowed for B-frames",
+          0, 51, GST_OMX_VIDEO_ENC_MIN_QP_DEFAULT,
+          G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS |
+          GST_PARAM_MUTABLE_READY));
+
+  g_object_class_install_property (gobject_class, PROP_MAX_QUANT_B_FRAMES,
+      g_param_spec_uint ("max-quant-b-frames", "max B-Frame Quantization",
+          "Maximum quantization parameter allowed for B-frames",
+          0, 51, GST_OMX_VIDEO_ENC_MAX_QP_DEFAULT,
+          G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS |
+          GST_PARAM_MUTABLE_READY));
+
+  g_object_class_install_property (gobject_class, PROP_NUM_LTR_FRAMES,
+      g_param_spec_uint ("num-ltr-frames", "LTR Frames Count",
+          "Number of Long Term Reference Frames (0xffffffff=component default)",
+          0, G_MAXUINT, GST_OMX_VIDEO_ENC_NUM_LTR_FRAMES_DEFAULT,
+          G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS |
+          GST_PARAM_MUTABLE_READY));
+
+  g_object_class_install_property (gobject_class, PROP_NUM_HIER_LAYERS,
+      g_param_spec_uint ("num-hierarchical-layers", "Hierarchical Layers Count",
+          "Number of Hierarchical Layers (0xffffffff=component default)",
+          0, G_MAXUINT, GST_OMX_VIDEO_ENC_NUM_HIER_LAYERS_DEFAULT,
+          G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS |
+          GST_PARAM_MUTABLE_READY));
+#endif // USE_OMX_TARGET_QTI
+
 #ifdef USE_OMX_TARGET_ZYNQ_USCALE_PLUS
   g_object_class_install_property (gobject_class, PROP_QP_MODE,
       g_param_spec_enum ("qp-mode", "QP mode",
@@ -506,9 +611,20 @@ gst_omx_video_enc_init (GstOMXVideoEnc * self)
 {
   self->control_rate = GST_OMX_VIDEO_ENC_CONTROL_RATE_DEFAULT;
   self->target_bitrate = GST_OMX_VIDEO_ENC_TARGET_BITRATE_DEFAULT;
+  self->rotate = GST_OMX_VIDEO_ENC_ROTATE_DEFAULT;
   self->quant_i_frames = GST_OMX_VIDEO_ENC_QUANT_I_FRAMES_DEFAULT;
   self->quant_p_frames = GST_OMX_VIDEO_ENC_QUANT_P_FRAMES_DEFAULT;
   self->quant_b_frames = GST_OMX_VIDEO_ENC_QUANT_B_FRAMES_DEFAULT;
+#ifdef USE_OMX_TARGET_QTI
+  self->min_quant_i_frames = GST_OMX_VIDEO_ENC_MIN_QP_DEFAULT;
+  self->max_quant_i_frames = GST_OMX_VIDEO_ENC_MAX_QP_DEFAULT;
+  self->min_quant_p_frames = GST_OMX_VIDEO_ENC_MIN_QP_DEFAULT;
+  self->max_quant_p_frames = GST_OMX_VIDEO_ENC_MAX_QP_DEFAULT;
+  self->min_quant_b_frames = GST_OMX_VIDEO_ENC_MIN_QP_DEFAULT;
+  self->max_quant_b_frames = GST_OMX_VIDEO_ENC_MAX_QP_DEFAULT;
+  self->num_ltr_frames = GST_OMX_VIDEO_ENC_NUM_LTR_FRAMES_DEFAULT;
+  self->num_hier_layers = GST_OMX_VIDEO_ENC_NUM_HIER_LAYERS_DEFAULT;
+#endif
 #ifdef USE_OMX_TARGET_ZYNQ_USCALE_PLUS
   self->qp_mode = GST_OMX_VIDEO_ENC_QP_MODE_DEFAULT;
   self->min_qp = GST_OMX_VIDEO_ENC_MIN_QP_DEFAULT;
@@ -920,11 +1036,221 @@ gst_omx_video_enc_open (GstVideoEncoder * encoder)
       }
     }
   }
+#ifdef USE_OMX_TARGET_QTI
+  {
+    OMX_ERRORTYPE err;
+    // TODO Should be PrependSPSPPSToIDRFramesParams but the struct is defined
+    // in HardwareAPI.h which is C++ header and can not be included here.
+    OMX_CONFIG_BOOLEANTYPE prepend_sps_pps_headers;
+    OMX_QCOM_VIDEO_CONFIG_AUD audelimiter;
+    OMX_QCOM_VIDEO_PARAM_IPB_QPRANGETYPE qprange;
+
+    GST_OMX_INIT_STRUCT (&prepend_sps_pps_headers);
+    prepend_sps_pps_headers.bEnabled = OMX_TRUE;
+
+    err =
+        gst_omx_component_set_parameter (self->enc,
+        OMX_QcomIndexParamSequenceHeaderWithIDR, &prepend_sps_pps_headers);
+    if (err == OMX_ErrorUnsupportedIndex) {
+      GST_WARNING_OBJECT (self,
+          "Setting OMX_QcomIndexParamSequenceHeaderWithIDR not supported by the component");
+    } else if (err == OMX_ErrorUnsupportedSetting) {
+      GST_WARNING_OBJECT (self,
+          "Setting SPS/PPS header with IDR frames not supported by the component");
+    } else if (err != OMX_ErrorNone) {
+      GST_ERROR_OBJECT (self,
+          "Failed to set OMX_QcomIndexParamSequenceHeaderWithIDR: %s (0x%08x)",
+          gst_omx_error_to_string (err), err);
+      return FALSE;
+    }
+
+    GST_OMX_INIT_STRUCT (&audelimiter);
+    audelimiter.bEnable = OMX_TRUE;
+
+    err =
+        gst_omx_component_set_parameter (self->enc,
+        OMX_QcomIndexParamAUDelimiter, &audelimiter);
+    if (err == OMX_ErrorUnsupportedIndex) {
+      GST_WARNING_OBJECT (self,
+          "Setting OMX_QcomIndexParamAUDelimiter not supported by the component");
+    } else if (err == OMX_ErrorUnsupportedSetting) {
+      GST_WARNING_OBJECT (self,
+          "Setting AU Delimiter not supported by the component");
+    } else if (err != OMX_ErrorNone) {
+      GST_ERROR_OBJECT (self,
+          "Failed to set OMX_QcomIndexParamAUDelimiter: %s (0x%08x)",
+          gst_omx_error_to_string (err), err);
+      return FALSE;
+    }
+
+    GST_OMX_INIT_STRUCT (&qprange);
+    qprange.nPortIndex = self->enc_out_port->index;
+
+    qprange.minIQP = self->min_quant_i_frames;
+    qprange.maxIQP = self->max_quant_i_frames;
+    qprange.minPQP = self->min_quant_p_frames;
+    qprange.maxPQP = self->max_quant_p_frames;
+    qprange.minBQP = self->min_quant_b_frames;
+    qprange.maxBQP = self->max_quant_b_frames;
+
+    err =
+        gst_omx_component_set_parameter (self->enc,
+        OMX_QcomIndexParamVideoIPBQPRange, &qprange);
+    if (err == OMX_ErrorUnsupportedIndex) {
+      GST_WARNING_OBJECT (self,
+          "Setting OMX_QcomIndexParamVideoIPBQPRange not supported by the component");
+    } else if (err == OMX_ErrorUnsupportedSetting) {
+      GST_WARNING_OBJECT (self,
+          "Setting IPB quantization range not supported by the component");
+    } else if (err != OMX_ErrorNone) {
+      GST_ERROR_OBJECT (self,
+          "Failed to set OMX_QcomIndexParamVideoIPBQPRange: %s (0x%08x)",
+          gst_omx_error_to_string (err), err);
+      return FALSE;
+    }
+
+    if (self->num_ltr_frames != 0xffffffff) {
+      QOMX_VIDEO_PARAM_LTRCOUNT_TYPE ltrframes;
+
+      GST_OMX_INIT_STRUCT (&ltrframes);
+      ltrframes.nPortIndex = self->enc_out_port->index;
+
+      err =
+          gst_omx_component_get_parameter (self->enc,
+          QOMX_IndexParamVideoLTRCount, &ltrframes);
+
+      if (err == OMX_ErrorNone) {
+
+        ltrframes.nCount = self->num_ltr_frames;
+
+        err =
+            gst_omx_component_set_parameter (self->enc,
+            QOMX_IndexParamVideoLTRCount, &ltrframes);
+        if (err == OMX_ErrorUnsupportedIndex) {
+          GST_WARNING_OBJECT (self,
+              "Setting QOMX_IndexParamVideoLTRCount not supported by the component");
+        } else if (err == OMX_ErrorUnsupportedSetting) {
+          GST_WARNING_OBJECT (self,
+              "Setting LTR frames count not supported by the component");
+        } else if (err != OMX_ErrorNone) {
+          GST_ERROR_OBJECT (self,
+              "Failed to set QOMX_IndexParamVideoLTRCount: %s (0x%08x)",
+              gst_omx_error_to_string (err), err);
+          return FALSE;
+        }
+      } else {
+        GST_ERROR_OBJECT (self,
+            "Failed to get LTR frames parameters: %s (0x%08x)",
+            gst_omx_error_to_string (err), err);
+
+      }
+    }
+
+    if (self->num_hier_layers != 0xffffffff) {
+      OMX_VIDEO_PARAM_ANDROID_TEMPORALLAYERINGTYPE layerparams;
+
+      GST_OMX_INIT_STRUCT (&layerparams);
+      layerparams.nPortIndex = self->enc_out_port->index;
+
+      err =
+          gst_omx_component_get_parameter (self->enc,
+              (OMX_INDEXTYPE)OMX_IndexParamAndroidVideoTemporalLayering,
+              &layerparams);
+
+      if (err == OMX_ErrorNone) {
+        layerparams.nLayerCountMax = self->num_hier_layers;
+        layerparams.nPLayerCountActual = self->num_hier_layers;
+        layerparams.nBLayerCountActual = 0;
+        layerparams.bBitrateRatiosSpecified = OMX_FALSE;
+        layerparams.ePattern = layerparams.eSupportedPatterns;
+        GST_DEBUG_OBJECT (self,
+            "Setting %u hierarchical P layers", self->num_hier_layers);
+
+        err =
+            gst_omx_component_set_parameter (self->enc,
+            (OMX_INDEXTYPE)OMX_IndexParamAndroidVideoTemporalLayering,
+            &layerparams);
+
+        if (err == OMX_ErrorUnsupportedIndex) {
+          GST_WARNING_OBJECT (self,
+              "Setting OMX_IndexParamAndroidVideoTemporalLayering not supported by the component");
+        } else if (err == OMX_ErrorUnsupportedSetting) {
+          GST_WARNING_OBJECT (self,
+              "Setting hierarchical layers not supported by the component");
+        } else if (err != OMX_ErrorNone) {
+          GST_ERROR_OBJECT (self,
+              "Failed to set OMX_IndexParamAndroidVideoTemporalLayering: %s (0x%08x)",
+              gst_omx_error_to_string (err), err);
+          return FALSE;
+        }
+      } else {
+        GST_ERROR_OBJECT (self,
+            "Failed to get hierarchical layers parameters: %s (0x%08x)",
+            gst_omx_error_to_string (err), err);
+
+      }
+    }
+  }
+#endif // USE_OMX_TARGET_QTI
+
 #ifdef USE_OMX_TARGET_ZYNQ_USCALE_PLUS
   if (!set_zynqultrascaleplus_props (self))
     return FALSE;
 #endif
 
+  {
+    OMX_ERRORTYPE err;
+    OMX_CONFIG_ROTATIONTYPE rparams;
+    OMX_PARAM_U32TYPE priority;
+
+    /* Get and set input image rotation */
+    GST_OMX_INIT_STRUCT (&rparams);
+    rparams.nPortIndex = self->enc_out_port->index;
+
+    err =
+        gst_omx_component_get_config (GST_OMX_VIDEO_ENC (self)->enc,
+        OMX_IndexConfigCommonRotate, &rparams);
+    if (err == OMX_ErrorUnsupportedIndex) {
+      GST_WARNING_OBJECT (self,
+          "OMX_IndexConfigCommonRotate not supported by component");
+      return TRUE;
+    } else if (err != OMX_ErrorNone) {
+      GST_ERROR_OBJECT (self,
+          "can't get OMX_IndexConfigCommonRotate %s (0x%08x)",
+          gst_omx_error_to_string (err), err);
+      return FALSE;
+    }
+
+    if ((self->rotate * 90) != rparams.nRotation) {
+      rparams.nRotation = self->rotate * 90;
+
+      err =
+          gst_omx_component_set_config (self->enc,
+          OMX_IndexConfigCommonRotate, &rparams);
+
+      GST_LOG_OBJECT (self, "Rotation set to %u", rparams.nRotation);
+      if (err != OMX_ErrorNone)
+        GST_ERROR_OBJECT (self,
+            "Failed to configure input image rotation: %s (0x%08x)",
+            gst_omx_error_to_string (err), err);
+    }
+
+    /* Set realtime session priority */
+    GST_OMX_INIT_STRUCT (&priority);
+    priority.nPortIndex = self->enc_out_port->index;
+    priority.nU32 = 0;
+
+    err =
+        gst_omx_component_set_config (self->enc,
+        OMX_IndexConfigPriority, &priority);
+
+    GST_LOG_OBJECT (self, "Session priority set to %u", priority.nU32);
+    if (err != OMX_ErrorNone)
+      GST_ERROR_OBJECT (self,
+          "Failed to configure realtime session priority: %s (0x%08x)",
+          gst_omx_error_to_string (err), err);
+  }
+
   return TRUE;
 }
 
@@ -1028,6 +1354,9 @@ gst_omx_video_enc_set_property (GObject * object, guint prop_id,
       }
       GST_OBJECT_UNLOCK (self);
       break;
+    case PROP_ROTATE:
+      self->rotate = g_value_get_enum (value);
+      break;
     case PROP_QUANT_I_FRAMES:
       self->quant_i_frames = g_value_get_uint (value);
       break;
@@ -1037,6 +1366,32 @@ gst_omx_video_enc_set_property (GObject * object, guint prop_id,
     case PROP_QUANT_B_FRAMES:
       self->quant_b_frames = g_value_get_uint (value);
       break;
+#ifdef USE_OMX_TARGET_QTI
+    case PROP_MIN_QUANT_I_FRAMES:
+      self->min_quant_i_frames = g_value_get_uint (value);
+      break;
+    case PROP_MAX_QUANT_I_FRAMES:
+      self->max_quant_i_frames = g_value_get_uint (value);
+      break;
+    case PROP_MIN_QUANT_P_FRAMES:
+      self->min_quant_p_frames = g_value_get_uint (value);
+      break;
+    case PROP_MAX_QUANT_P_FRAMES:
+      self->max_quant_p_frames = g_value_get_uint (value);
+      break;
+    case PROP_MIN_QUANT_B_FRAMES:
+      self->min_quant_b_frames = g_value_get_uint (value);
+      break;
+    case PROP_MAX_QUANT_B_FRAMES:
+      self->max_quant_b_frames = g_value_get_uint (value);
+      break;
+    case PROP_NUM_LTR_FRAMES:
+      self->num_ltr_frames = g_value_get_uint (value);
+      break;
+    case PROP_NUM_HIER_LAYERS:
+      self->num_hier_layers = g_value_get_uint (value);
+      break;
+#endif
 #ifdef USE_OMX_TARGET_ZYNQ_USCALE_PLUS
     case PROP_QP_MODE:
       self->qp_mode = g_value_get_enum (value);
@@ -1108,6 +1463,9 @@ gst_omx_video_enc_get_property (GObject * object, guint prop_id, GValue * value,
       g_value_set_uint (value, self->target_bitrate);
       GST_OBJECT_UNLOCK (self);
       break;
+    case PROP_ROTATE:
+      g_value_set_enum (value, self->rotate);
+      break;
     case PROP_QUANT_I_FRAMES:
       g_value_set_uint (value, self->quant_i_frames);
       break;
@@ -1117,6 +1475,32 @@ gst_omx_video_enc_get_property (GObject * object, guint prop_id, GValue * value,
     case PROP_QUANT_B_FRAMES:
       g_value_set_uint (value, self->quant_b_frames);
       break;
+#ifdef USE_OMX_TARGET_QTI
+    case PROP_MIN_QUANT_I_FRAMES:
+      g_value_set_uint (value, self->min_quant_i_frames);
+      break;
+    case PROP_MAX_QUANT_I_FRAMES:
+      g_value_set_uint (value, self->max_quant_i_frames);
+      break;
+    case PROP_MIN_QUANT_P_FRAMES:
+      g_value_set_uint (value, self->min_quant_p_frames);
+      break;
+    case PROP_MAX_QUANT_P_FRAMES:
+      g_value_set_uint (value, self->max_quant_p_frames);
+      break;
+    case PROP_MIN_QUANT_B_FRAMES:
+      g_value_set_uint (value, self->min_quant_b_frames);
+      break;
+    case PROP_MAX_QUANT_B_FRAMES:
+      g_value_set_uint (value, self->max_quant_b_frames);
+      break;
+    case PROP_NUM_LTR_FRAMES:
+      g_value_set_uint (value, self->num_ltr_frames);
+      break;
+    case PROP_NUM_HIER_LAYERS:
+      g_value_set_uint (value, self->num_hier_layers);
+      break;
+#endif
 #ifdef USE_OMX_TARGET_ZYNQ_USCALE_PLUS
     case PROP_QP_MODE:
       g_value_set_enum (value, self->qp_mode);
diff --git a/omx/gstomxvideoenc.h b/omx/gstomxvideoenc.h
index 2209534..b066285 100644
--- a/omx/gstomxvideoenc.h
+++ b/omx/gstomxvideoenc.h
@@ -72,9 +72,20 @@ struct _GstOMXVideoEnc
   /* properties */
   guint32 control_rate;
   guint32 target_bitrate; /* protected by object lock */
+  guint32 rotate;
   guint32 quant_i_frames;
   guint32 quant_p_frames;
   guint32 quant_b_frames;
+#ifdef USE_OMX_TARGET_QTI
+  guint32 min_quant_i_frames;
+  guint32 max_quant_i_frames;
+  guint32 min_quant_p_frames;
+  guint32 max_quant_p_frames;
+  guint32 min_quant_b_frames;
+  guint32 max_quant_b_frames;
+  guint32 num_ltr_frames;
+  guint32 num_hier_layers;
+#endif
 #ifdef USE_OMX_TARGET_ZYNQ_USCALE_PLUS
   guint32 qp_mode;
   guint32 min_qp;
-- 
2.17.1

