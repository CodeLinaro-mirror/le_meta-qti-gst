From c5e62de703055118d8d4797b5749c8dd9ea2d533 Mon Sep 17 00:00:00 2001
From: Amrutha Sai Gattu <agattu@codeaurora.org>
Date: Fri, 1 Nov 2019 09:56:20 +0530
Subject: [PATCH] waylandsink: support for scaler protocol

---
 configure.ac                 |   6 ++
 ext/wayland/Makefile.am      |   7 +++
 ext/wayland/gstwaylandsink.c | 108 ++++++++++++++++++++++++++++++++++-
 ext/wayland/gstwaylandsink.h |   1 +
 ext/wayland/wldisplay.c      |  19 +++++-
 ext/wayland/wldisplay.h      |   6 ++
 ext/wayland/wlwindow.c       | 100 ++++++++++++++++++++++++++------
 ext/wayland/wlwindow.h       |  13 +++--
 8 files changed, 236 insertions(+), 24 deletions(-)

diff --git a/configure.ac b/configure.ac
index a9d7efe2d..e756094b7 100644
--- a/configure.ac
+++ b/configure.ac
@@ -1338,6 +1338,12 @@ AC_ARG_ENABLE([xdg-shell-protocol],
       [use_xdg_shell_protocol="${enableval}"])
 AM_CONDITIONAL([XDG_SHELL_PROTOCOL], [test "x${use_xdg_shell_protocol}" = "xyes"])
 
+AC_ARG_ENABLE([scaler-protocol],
+   AC_HELP_STRING([--enable-scaler-protocol],
+      [Use scaler protocol [default=no]]),
+      [use_scaler_protocol="${enableval}"])
+AM_CONDITIONAL([SCALER_PROTOCOL], [test "x${use_scaler_protocol}" = "xyes"])
+
 dnl **** WebP ****
 translit(dnm, m, l) AM_CONDITIONAL(USE_WEBP, true)
 AG_GST_CHECK_FEATURE(WEBP, [WebP], webp , [
diff --git a/ext/wayland/Makefile.am b/ext/wayland/Makefile.am
index 6d93c190b..eb328daae 100644
--- a/ext/wayland/Makefile.am
+++ b/ext/wayland/Makefile.am
@@ -39,6 +39,13 @@ nodist_libgstwaylandsink_la_SOURCES += xdg-shell-unstable-v6-protocol.c
 libgstwaylandsink_la_CFLAGS += -D__XDG_SHELL_PROTOCOL
 endif
 
+if SCALER_PROTOCOL
+BUILT_SOURCES += scaler-protocol.c \
+                 scaler-client-protocol.h
+nodist_libgstwaylandsink_la_SOURCES += scaler-protocol.c
+libgstwaylandsink_la_CFLAGS += -D__SCALER_PROTOCOL
+endif
+
 noinst_HEADERS = \
 	gstwaylandsink.h \
 	wlshmallocator.h \
diff --git a/ext/wayland/gstwaylandsink.c b/ext/wayland/gstwaylandsink.c
index d6dd450d6..9394672f6 100644
--- a/ext/wayland/gstwaylandsink.c
+++ b/ext/wayland/gstwaylandsink.c
@@ -63,7 +63,11 @@ enum
   PROP_0,
   PROP_DISPLAY,
   PROP_XDG_SHELL,
-  PROP_FULLSCREEN
+  PROP_FULLSCREEN,
+  PROP_X,
+  PROP_Y,
+  PROP_DST_WIDTH,
+  PROP_DST_HEIGHT,
 };
 
 GST_DEBUG_CATEGORY (gstwayland_debug);
@@ -215,6 +219,26 @@ gst_wayland_sink_class_init (GstWaylandSinkClass * klass)
           "Whether the surface should be made fullscreen ", FALSE,
           G_PARAM_READWRITE | G_PARAM_CONSTRUCT | G_PARAM_STATIC_STRINGS |
           GST_PARAM_MUTABLE_PLAYING));
+
+  g_object_class_install_property (gobject_class, PROP_X,
+      g_param_spec_uint ("x", "X Position", "X postion for the content",
+          0, 1920, 0, G_PARAM_READWRITE | G_PARAM_CONSTRUCT |
+          G_PARAM_STATIC_STRINGS | GST_PARAM_MUTABLE_PLAYING));
+
+  g_object_class_install_property (gobject_class, PROP_Y,
+      g_param_spec_uint ("y", "Y Position", "Y postion for the content",
+          0, 1080, 0, G_PARAM_READWRITE | G_PARAM_CONSTRUCT |
+          G_PARAM_STATIC_STRINGS | GST_PARAM_MUTABLE_PLAYING));
+
+  g_object_class_install_property (gobject_class, PROP_DST_WIDTH,
+      g_param_spec_uint ("width", "Destination Width", "Destination Width for "
+          "the content", 0, 1920, 640, G_PARAM_READWRITE | G_PARAM_CONSTRUCT |
+          G_PARAM_STATIC_STRINGS | GST_PARAM_MUTABLE_PLAYING));
+
+  g_object_class_install_property (gobject_class, PROP_DST_HEIGHT,
+      g_param_spec_uint ("height", "Destination Height", "Destination Height for"
+          " the content", 0, 1080, 480, G_PARAM_READWRITE | G_PARAM_CONSTRUCT |
+          G_PARAM_STATIC_STRINGS | GST_PARAM_MUTABLE_PLAYING));
 }
 
 static void
@@ -238,6 +262,40 @@ gst_wayland_sink_set_fullscreen (GstWaylandSink * sink, gboolean fullscreen)
   g_mutex_unlock (&sink->render_lock);
 }
 
+static void
+gst_wayland_sink_set_position (GstWaylandSink * sink, guint x, guint y)
+{
+  if ((x == sink->video_rectangle.x) && (y == sink->video_rectangle.y))
+    return;
+
+  g_mutex_lock (&sink->render_lock);
+
+  sink->video_rectangle.x = x;
+  sink->video_rectangle.y = y;
+
+  if (!sink->fullscreen)
+    gst_wl_window_ensure_position (sink->window, x, y);
+
+  g_mutex_unlock (&sink->render_lock);
+}
+
+static void
+gst_wayland_sink_set_dimensions (GstWaylandSink * sink, guint w, guint h)
+{
+  if ((w == sink->video_rectangle.w) && (h == sink->video_rectangle.h))
+    return;
+
+  g_mutex_lock (&sink->render_lock);
+
+  sink->video_rectangle.w = w;
+  sink->video_rectangle.h = h;
+
+  if (!sink->fullscreen)
+    gst_wl_window_ensure_dimensions (sink->window, w, h);
+
+  g_mutex_unlock (&sink->render_lock);
+}
+
 static void
 gst_wayland_sink_get_property (GObject * object,
     guint prop_id, GValue * value, GParamSpec * pspec)
@@ -260,6 +318,26 @@ gst_wayland_sink_get_property (GObject * object,
       g_value_set_boolean (value, sink->fullscreen);
       GST_OBJECT_UNLOCK (sink);
       break;
+    case PROP_X:
+      GST_OBJECT_LOCK (sink);
+      g_value_set_uint (value, sink->video_rectangle.x);
+      GST_OBJECT_UNLOCK (sink);
+      break;
+    case PROP_Y:
+      GST_OBJECT_LOCK (sink);
+      g_value_set_uint (value, sink->video_rectangle.y);
+      GST_OBJECT_UNLOCK (sink);
+      break;
+    case PROP_DST_WIDTH:
+      GST_OBJECT_LOCK (sink);
+      g_value_set_uint (value, sink->video_rectangle.w);
+      GST_OBJECT_UNLOCK (sink);
+      break;
+    case PROP_DST_HEIGHT:
+      GST_OBJECT_LOCK (sink);
+      g_value_set_uint (value, sink->video_rectangle.h);
+      GST_OBJECT_UNLOCK (sink);
+      break;
     default:
       G_OBJECT_WARN_INVALID_PROPERTY_ID (object, prop_id, pspec);
       break;
@@ -288,6 +366,30 @@ gst_wayland_sink_set_property (GObject * object,
       gst_wayland_sink_set_fullscreen (sink, g_value_get_boolean (value));
       GST_OBJECT_UNLOCK (sink);
       break;
+    case PROP_X:
+      GST_OBJECT_LOCK (sink);
+      gst_wayland_sink_set_position (sink, g_value_get_uint (value),
+          sink->video_rectangle.y);
+      GST_OBJECT_UNLOCK (sink);
+      break;
+    case PROP_Y:
+      GST_OBJECT_LOCK (sink);
+      gst_wayland_sink_set_position (sink, sink->video_rectangle.x,
+          g_value_get_uint (value));
+      GST_OBJECT_UNLOCK (sink);
+      break;
+    case PROP_DST_WIDTH:
+      GST_OBJECT_LOCK (sink);
+      gst_wayland_sink_set_dimensions (sink, g_value_get_uint (value),
+          sink->video_rectangle.h);
+      GST_OBJECT_UNLOCK (sink);
+      break;
+    case PROP_DST_HEIGHT:
+      GST_OBJECT_LOCK (sink);
+      gst_wayland_sink_set_dimensions (sink, sink->video_rectangle.w,
+          g_value_get_uint (value));
+      GST_OBJECT_UNLOCK (sink);
+      break;
     default:
       G_OBJECT_WARN_INVALID_PROPERTY_ID (object, prop_id, pspec);
       break;
@@ -706,8 +808,8 @@ gst_wayland_sink_show_frame (GstVideoSink * vsink, GstBuffer * buffer)
     if (!sink->window) {
       /* if we were not provided a window, create one ourselves */
       sink->window = gst_wl_window_new_toplevel (sink->display,
-          &sink->video_info, sink->fullscreen, sink->use_xdg_shell,
-          &sink->render_lock);
+          &sink->video_info, &sink->video_rectangle, sink->fullscreen,
+          sink->use_xdg_shell, &sink->render_lock);
     }
   }
 
diff --git a/ext/wayland/gstwaylandsink.h b/ext/wayland/gstwaylandsink.h
index e29b5087b..3083967e6 100644
--- a/ext/wayland/gstwaylandsink.h
+++ b/ext/wayland/gstwaylandsink.h
@@ -64,6 +64,7 @@ struct _GstWaylandSink
 
   gboolean use_xdg_shell;
   gchar *display_name;
+  GstVideoRectangle video_rectangle;
 
   gboolean redraw_pending;
   GMutex render_lock;
diff --git a/ext/wayland/wldisplay.c b/ext/wayland/wldisplay.c
index a29952fe6..b9c9d248b 100644
--- a/ext/wayland/wldisplay.c
+++ b/ext/wayland/wldisplay.c
@@ -81,6 +81,11 @@ gst_wl_display_finalize (GObject * gobject)
   if (self->viewporter)
     wp_viewporter_destroy (self->viewporter);
 
+#ifdef __SCALER_PROTOCOL
+  if (self->scaler)
+    wl_scaler_destroy (self->scaler);
+#endif
+
   if (self->shm)
     wl_shm_destroy (self->shm);
 
@@ -253,6 +258,11 @@ registry_handle_global (void *data, struct wl_registry *registry,
   } else if (g_strcmp0 (interface, "wp_viewporter") == 0) {
     self->viewporter =
         wl_registry_bind (registry, id, &wp_viewporter_interface, 1);
+#ifdef __SCALER_PROTOCOL
+  } else if (g_strcmp0(interface, "wl_scaler") == 0) {
+    self->scaler =
+        wl_registry_bind (registry, id, &wl_scaler_interface, MIN (version, 2));
+#endif
   } else if (g_strcmp0 (interface, "zwp_linux_dmabuf_v1") == 0) {
     self->dmabuf =
         wl_registry_bind (registry, id, &zwp_linux_dmabuf_v1_interface, 1);
@@ -368,7 +378,7 @@ gst_wl_display_new_existing (struct wl_display * display,
    * compositor that don't implement this extension. */
   if (!self->viewporter) {
     g_warning ("Wayland compositor is missing the ability to scale, video "
-        "display may not work properly.");
+        "display may not work properly. - No viewporter present");
   }
 
   if (!self->dmabuf) {
@@ -382,6 +392,13 @@ gst_wl_display_new_existing (struct wl_display * display,
   }
 #endif
 
+#ifdef __SCALER_PROTOCOL
+  if (!self->scaler) {
+    g_warning ("Wayland compositor is missing the ability to scale, video "
+        "display may not work properly - No scaler present");
+  }
+#endif
+
   self->thread = g_thread_try_new ("GstWlDisplay", gst_wl_display_thread_run,
       self, &err);
   if (err) {
diff --git a/ext/wayland/wldisplay.h b/ext/wayland/wldisplay.h
index 816c37d5a..155b1b138 100644
--- a/ext/wayland/wldisplay.h
+++ b/ext/wayland/wldisplay.h
@@ -29,6 +29,9 @@
 #ifdef __XDG_SHELL_PROTOCOL
 #include "xdg-shell-unstable-v6-client-protocol.h"
 #endif
+#ifdef __SCALER_PROTOCOL
+#include "scaler-client-protocol.h"
+#endif
 
 G_BEGIN_DECLS
 
@@ -60,6 +63,9 @@ struct _GstWlDisplay
 #endif
   struct wl_shm *shm;
   struct wp_viewporter *viewporter;
+#ifdef __SCALER_PROTOCOL
+  struct wl_scaler *scaler;
+#endif
   struct zwp_linux_dmabuf_v1 *dmabuf;
   GArray *shm_formats;
   GArray *dmabuf_formats;
diff --git a/ext/wayland/wlwindow.c b/ext/wayland/wlwindow.c
index 6305e4d0f..5540e9248 100644
--- a/ext/wayland/wlwindow.c
+++ b/ext/wayland/wlwindow.c
@@ -151,8 +151,13 @@ gst_wl_window_finalize (GObject * gobject)
   if (self->shell_surface)
     wl_shell_surface_destroy (self->shell_surface);
 
-  if (self->video_viewport)
-    wp_viewport_destroy (self->video_viewport);
+  if (self->wp_video_viewport)
+    wp_viewport_destroy (self->wp_video_viewport);
+
+#ifdef __SCALER_PROTOCOL
+  if (self->wl_video_viewport)
+    wl_viewport_destroy (self->wl_video_viewport);
+#endif
 
   wl_subsurface_destroy (self->video_subsurface);
   wl_surface_destroy (self->video_surface);
@@ -160,8 +165,13 @@ gst_wl_window_finalize (GObject * gobject)
   if (self->area_subsurface)
     wl_subsurface_destroy (self->area_subsurface);
 
-  if (self->area_viewport)
-    wp_viewport_destroy (self->area_viewport);
+  if (self->wp_area_viewport)
+    wp_viewport_destroy (self->wp_area_viewport);
+
+#ifdef __SCALER_PROTOCOL
+  if (self->wl_area_viewport)
+    wl_viewport_destroy (self->wl_area_viewport);
+#endif
 
   wl_surface_destroy (self->area_surface);
 
@@ -197,10 +207,17 @@ gst_wl_window_new_internal (GstWlDisplay * display, GMutex * render_lock)
   wl_subsurface_set_desync (window->video_subsurface);
 
   if (display->viewporter) {
-    window->area_viewport = wp_viewporter_get_viewport (display->viewporter,
+    window->wp_area_viewport = wp_viewporter_get_viewport (display->viewporter,
+        window->area_surface);
+    window->wp_video_viewport = wp_viewporter_get_viewport (display->viewporter,
+        window->video_surface);
+#ifdef __SCALER_PROTOCOL
+  } else if (display->scaler) {
+    window->wl_area_viewport = wl_scaler_get_viewport (display->scaler,
         window->area_surface);
-    window->video_viewport = wp_viewporter_get_viewport (display->viewporter,
+    window->wl_video_viewport = wl_scaler_get_viewport (display->scaler,
         window->video_surface);
+#endif
   }
 
   /* do not accept input */
@@ -238,12 +255,33 @@ gst_wl_window_ensure_fullscreen (GstWlWindow * window, gboolean fullscreen)
   }
 }
 
+void
+gst_wl_window_ensure_position (GstWlWindow * window, guint x, guint y)
+{
+  if (!window)
+    return;
+
+#ifdef __XDG_SHELL_PROTOCOL
+  if (window->xdg_surface && window->xdg_toplevel)
+    zxdg_toplevel_v6_set_position (window->xdg_toplevel, x, y);
+#endif
+}
+
+void
+gst_wl_window_ensure_dimensions (GstWlWindow * window, guint width, guint height)
+{
+  if (!window)
+    return;
+
+  gst_wl_window_set_render_rectangle (window, 0, 0, width, height);
+}
+
 GstWlWindow *
 gst_wl_window_new_toplevel (GstWlDisplay * display, const GstVideoInfo * info,
-    gboolean fullscreen, gboolean xdgshell, GMutex * render_lock)
+    const GstVideoRectangle * rectangle, gboolean fullscreen, gboolean xdgshell,
+    GMutex * render_lock)
 {
   GstWlWindow *window;
-  gint width;
 
   window = gst_wl_window_new_internal (display, render_lock);
 
@@ -313,10 +351,25 @@ gst_wl_window_new_toplevel (GstWlDisplay * display, const GstVideoInfo * info,
     GST_INFO ("Using wl_shell_surface");
   }
 
-  /* set the initial size to be the same as the reported video size */
-  width =
-      gst_util_uint64_scale_int_round (info->width, info->par_n, info->par_d);
-  gst_wl_window_set_render_rectangle (window, 0, 0, width, info->height);
+  if ((rectangle->h != 0) && (rectangle->w != 0) && !fullscreen) {
+    gst_wl_window_set_render_rectangle (window, 0, 0, rectangle->w,
+         rectangle->h);
+#ifdef __XDG_SHELL_PROTOCOL
+    if (window->xdg_surface && window->xdg_toplevel)
+      zxdg_toplevel_v6_set_position (window->xdg_toplevel, rectangle->x,
+          rectangle->y);
+#endif
+  } else {
+    /* set the initial size to be the same as the reported video size */
+    gint width = gst_util_uint64_scale_int_round (
+        info->width, info->par_n, info->par_d);
+    gst_wl_window_set_render_rectangle (window, 0, 0, width, info->height);
+
+#ifdef __XDG_SHELL_PROTOCOL
+    if (window->xdg_surface && window->xdg_toplevel)
+      zxdg_toplevel_v6_set_position (window->xdg_toplevel, 0, 0);
+#endif
+  }
 
   return window;
 }
@@ -379,9 +432,14 @@ gst_wl_window_resize_video_surface (GstWlWindow * window, gboolean commit)
   dst.w = window->render_rectangle.w;
   dst.h = window->render_rectangle.h;
 
-  if (window->video_viewport) {
+  if (window->wp_video_viewport) {
+    gst_video_sink_center_rect (src, dst, &res, TRUE);
+    wp_viewport_set_destination (window->wp_video_viewport, res.w, res.h);
+#ifdef __SCALER_PROTOCOL
+  } else if (window->wl_video_viewport) {
     gst_video_sink_center_rect (src, dst, &res, TRUE);
-    wp_viewport_set_destination (window->video_viewport, res.w, res.h);
+    wl_viewport_set_destination (window->wl_video_viewport, res.w, res.h);
+#endif
   } else {
     gst_video_sink_center_rect (src, dst, &res, FALSE);
   }
@@ -484,6 +542,11 @@ gst_wl_window_update_borders (GstWlWindow * window)
   if (window->display->viewporter) {
     width = height = 1;
     window->no_border_update = TRUE;
+#ifdef __SCALER_PROTOCOL
+  } else if (window->display->scaler) {
+    width = height = 1;
+    window->no_border_update = TRUE;
+#endif
   } else {
     width = window->render_rectangle.w;
     height = window->render_rectangle.h;
@@ -531,8 +594,13 @@ gst_wl_window_set_render_rectangle (GstWlWindow * window, gint x, gint y,
     wl_subsurface_set_position (window->area_subsurface, x, y);
 
   /* change the size of the area */
-  if (window->area_viewport)
-    wp_viewport_set_destination (window->area_viewport, w, h);
+  if (window->wp_area_viewport) {
+    wp_viewport_set_destination (window->wp_area_viewport, w, h);
+#ifdef __SCALER_PROTOCOL
+  } else if (window->wl_area_viewport) {
+    wl_viewport_set_destination (window->wl_area_viewport, w, h);
+#endif
+  }
 
   gst_wl_window_update_borders (window);
 
diff --git a/ext/wayland/wlwindow.h b/ext/wayland/wlwindow.h
index 9305deba4..3c3fb4889 100644
--- a/ext/wayland/wlwindow.h
+++ b/ext/wayland/wlwindow.h
@@ -46,10 +46,14 @@ struct _GstWlWindow
   GstWlDisplay *display;
   struct wl_surface *area_surface;
   struct wl_subsurface *area_subsurface;
-  struct wp_viewport *area_viewport;
   struct wl_surface *video_surface;
   struct wl_subsurface *video_subsurface;
-  struct wp_viewport *video_viewport;
+  struct wp_viewport *wp_area_viewport;
+  struct wp_viewport *wp_video_viewport;
+#ifdef __SCALER_PROTOCOL
+  struct wl_viewport *wl_area_viewport;
+  struct wl_viewport *wl_video_viewport;
+#endif
   struct wl_shell_surface *shell_surface;
 #ifdef __XDG_SHELL_PROTOCOL
   struct zxdg_surface_v6 *xdg_surface;
@@ -83,9 +87,10 @@ GType gst_wl_window_get_type (void);
 
 void gst_wl_window_ensure_fullscreen (GstWlWindow * window,
         gboolean fullscreen);
+void gst_wl_window_ensure_position (GstWlWindow * window, guint x, guint y);
 GstWlWindow *gst_wl_window_new_toplevel (GstWlDisplay * display,
-        const GstVideoInfo * info, gboolean fullscreen, gboolean xdgshell,
-        GMutex * render_lock);
+        const GstVideoInfo * info, const GstVideoRectangle * rectangle,
+        gboolean fullscreen, gboolean xdgshell, GMutex * render_lock);
 GstWlWindow *gst_wl_window_new_in_surface (GstWlDisplay * display,
         struct wl_surface * parent, GMutex * render_lock);
 
-- 
2.17.1

